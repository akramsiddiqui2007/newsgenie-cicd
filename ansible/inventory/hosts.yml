all:
  hosts:
    newsgenie:
      ansible_host: 35.238.73.72
      ansible_user: akram
      ansible_ssh_private_key_file: "{{ lookup('env', 'ANSIBLE_SSH_PRIVATE_KEY_FILE') | default('/home/akram/.ssh/newsgenie_oci', true) }}"
      ansible_python_interpreter: /usr/bin/python3
